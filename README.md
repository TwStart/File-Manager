# File-Manager
file-manager is a lightweight and efficient Python tool designed to simplify file and directory management. Unlike other libraries, it uses a secure deletion system that avoids common Windows permission locks (such as those caused by VS Code) by using the Recycle Bin.
